import styles from './Inflation.module.css'

type Props = {}

const Inflation = (props: Props) => {
  return (
    <div className={styles.container}>Inflation Fee</div>
  )
}

export default Inflation